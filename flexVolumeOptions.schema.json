{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FlexVolume Options",
  "type": "object",
  "properties": {
    "nasHostname": {
      "type": "string",
      "description": "The hostname of the NAS device node, as would be reported by invoking `hostname` there."
    },
    "nasLocalPath": {
      "type": "string",
      "description": "The local path on the NAS device for direct mount."
    },
    "nasShare": {
      "type": "string",
      "description": "The CIFS share path for the NAS device in the rest of the cluster."
    },
    "mountOptions": {
      "type": "string",
      "description": "Additional mount options for the CIFS mount. See `man mount.cifs` for details."
    }
  },
  "required": ["nasHostname", "nasShare", "nasLocalPath"],
  "additionalProperties": true
}
